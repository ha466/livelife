import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../OneFile.jsx'; // Imports the core laboratory web application

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
